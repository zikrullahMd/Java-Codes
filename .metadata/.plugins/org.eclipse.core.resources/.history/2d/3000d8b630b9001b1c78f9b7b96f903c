import java.util.*;
import java.math.*;
import java.util.Map.Entry;
class Misc{
	
    public static void main(String[] args){
        Scanner in = new Scanner(System.in);
        int n = in.nextInt();
        String[] a = new String[n];
        for(int i = 0;i<n;i++) {
        	a[i]  = in.next();
        }
        Stack<String> st = new Stack<>();
        int res = 0;
        for(int i = 0;i<n;i++) {
        	String s = a[i];
        	if(s.charAt(0) >= '0' && s.charAt(0) <= '9') {
        		st.push(a[i]);
        	}else {
        		int one = Integer.parseInt(st.pop());
        		int two = Integer.parseInt(st.pop());
        		switch(s) {
        		case "+" : res = one + two;
        		break;
        		case "-" : res = one - two;
        		break;
        		case "*" : res = one * two;
        		break;
        		case "/" : res = Math.max(one, two) / Math.min(one, two);
        		break;
        		}
        	}
        }
        System.out.println(res);
    }
}
