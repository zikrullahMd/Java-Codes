import java.util.*;
import java.math.*;
import java.util.Map.Entry;
class Misc{
	
    public static void main(String[] args){
        Scanner in = new Scanner(System.in);
        int n = in.nextInt();
        int m = in.nextInt();
        int[][]  a= new int[n][m];
        for(int i = 0;i<n;i++) {
        	for(int j = 0;j<m;j++) {
        		a[i][j] = in.nextInt();
        	}
        }
        int k = in.nextInt();
        boolean found = false;
        int i = 0;
        int j = m-1;
        while(i<n && j>=0) {
        	if(a[i][j] == k) {
        		found = true;
        		break;
        	}else if(a[i][j]<k) {
        		i++;
        	}else {
        		j--;
        	}
        }
        System.out.println(!found ? "not found" : "found at "+i+","+j);
    }
} 