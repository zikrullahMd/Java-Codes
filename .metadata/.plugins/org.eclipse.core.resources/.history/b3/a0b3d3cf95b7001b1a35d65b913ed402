import java.util.*;
import java.math.*;
import java.util.Map.Entry;
class Misc{
	
    public static void main(String[] args){
        Scanner in = new Scanner(System.in);
        
        ArrayList<Integer> list = new ArrayList<>();
        Solution s = new Solution();
        while(true) {
        	char e = in.next().charAt(0);
        	if(e == 'q') {
        		break;
        	}
        	if(e == 'P') {
        		s.push(in.nextInt());
        	}else if(e == 'g') {
        		list.add(s.getMin());
        	}else {
        		s.pop();
        	}
        }
        for(int i : list) {
        	System.out.print(i+" ");
        }
    }
}

class Solution {
    Stack<Integer> st = new Stack<>();
    List<Integer> list = new ArrayList(Integer.MAX_VALUE);
    Stack<Integer> min = new Stack<Integer>(list);
    
    public void push(int x) {
        st.push(x);
        
        if(!min.isEmpty()){
            min.push(Math.min(x,min.peek()));
        }
    }

    public void pop() {
        if(!st.isEmpty() && !min.isEmpty()){
            st.pop();
            min.pop();
        }
    }

    public int top() {
        if(!st.isEmpty()){
            return st.peek();
        }
        return -1;
    }

    public int getMin() {
        if(!min.isEmpty()){
            return min.peek();
        }
        return -1;
    }
}
