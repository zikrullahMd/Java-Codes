import java.io.*;
import java.util.*;

public class Misc {
	
	public static int getRank(int[] a, int n) {
		
		Set<Integer> set = new HashSet<>(Arrays.stream(array).collect(Collectors.toSet()));
		
		
		
		
		return 0;
	}

    public static void main(String[] arga) {
         Scanner in = new Scanner(System.in);
         long t = in.nextLong();
         while(t-->0) {
             int n = in.nextInt();
             int[] a = new int[n];
             for(int i = 0;i<n;i++) {
                 a[i] = in.nextInt();
             }
             int q = in.nextInt();
             while(q-->0) {
            	 int m = in.nextInt();
            	 
             }
         }
    }
}